# =============================================================================
# ReconPilot — Python runtime dependencies
# =============================================================================
# Python 3.10+ is required by the codebase.
#
# Recommended (isolated environment):
#   python3 -m venv .venv
#   source .venv/bin/activate
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt
#
# Kali/Debian systems using PEP 668 can also install to the user site with:
#   python3 -m pip install --user --break-system-packages -r requirements.txt
# =============================================================================

# Qt desktop interface and Qt signals used by the scan manager.
PySide6>=6.6

# HTTP requests used by live-host fallback, header checks, JS collection,
# and the local Ollama AI Advisor client.
requests>=2.31
urllib3>=2.0

# HTML parsing used by the JavaScript collector.
beautifulsoup4>=4.12

# Asynchronous DNS enumeration.
dnspython>=2.4

# X.509 parsing used by the SSL/TLS certificate module.
cryptography>=41.0

# -----------------------------------------------------------------------------
# No Python SDK is required for AI Advisor: ReconPilot talks to a local Ollama
# server through `requests`.
#
# External CLI/runtime dependencies are installed/checked by install.sh:
#   iputils-ping (Linux ping), nmap, subfinder, httpx-toolkit, whatweb,
#   wafw00f, nuclei, gospider, feroxbuster, ffuf, and seclists.
#
# Optional URL-harvest boosters:
#   gau, waybackurls
#
# Ollama itself is optional and only needed when using AI Advisor.
# -----------------------------------------------------------------------------
